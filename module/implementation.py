import numpy as np


# This is an improvement version of what was on FunSpeech, coded by me.

def compute_power_per_bands(audio, bands=np.array([150, 551, 1051, 1901, 3501, 6501])):

    # TODO...
    return

def compute_powers():
    #...

def norm_volume(signal):
    return np.sqrt(np.sum(np.square(signal)) / signal.size) * signal




